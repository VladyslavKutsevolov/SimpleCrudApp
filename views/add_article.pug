extends layout

block content
    form.form-group(method="POST" action="/article/add")
        label(for= "title") Title
        input#title.form-control(name="title", type="text")
        br
        label(for= "body") Body
        textarea#body.form-control(name="body", type="text")
        br
        input.btn.btn-primary(value="Submit", type="submit")
