extends layout

block content
    .container
        h2 #{article.title}
        h5 Written by #{article.author}
        p #{article.body}
        a.btn.btn-outline-dark(href= "/articles/edit/" + article.id) Edit
        a.btn.btn-danger.ml-1.delete-article(href= "#", data-id= article.id) Delete